meta {
  name: SignIn
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: none
}

body:json {
  {
    "login": "000.000.000-00",
    "password":"senha123"
  }
}

vars:post-response {
  jwtToken: res.headers.authorization
}
